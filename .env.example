# Copy to .env and fill in. .env is gitignored.

# Default admin seed (only used the first time the DB is created and no users exist).
# Leave unset to have a random password generated and printed to the server logs once.
ADMIN_DEFAULT_USERNAME=admin
ADMIN_DEFAULT_PASSWORD=
